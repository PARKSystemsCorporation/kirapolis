{
  "name": "@kirapolis/desktop",
  "version": "0.1.0",
  "private": true,
  "main": "dist/main.js",
  "scripts": {
    "build": "npx tsc -p tsconfig.json",
    "typecheck": "npx tsc -p tsconfig.json --noEmit",
    "start": "electron dist/main.js"
  },
  "dependencies": {
    "@types/node": "^22.10.0",
    "typescript": "^5.7.0"
  }
}
